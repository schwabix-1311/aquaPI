# additional dependencies only needed to run the test suite (tests/),
# not required to run aquaPi itself - install with:
#   pip install -r requirements-dev.txt
-r requirements.txt

pytest>=7.4.0
pytest-mock>=3.11.1
# run the suite across all CPU cores: pytest -n auto (~4x faster, verified
# safe - no real QuestDB access, every INSTANCE_PATH is tmp_path-based)
pytest-xdist>=3.6.1

# needed so hist_nodes.QUEST_DB is True and hist_nodes.pg exists to be
# monkeypatched - tests never connect to a real QuestDB instance (no
# test uses the 'questdb' marker), but importing psycopg is required
# for the patched-pg test paths to even run
psycopg[binary]>=3.1.9
