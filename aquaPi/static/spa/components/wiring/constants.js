// Node-box geometry, shared by the canvas components (comps.js) and the
// pure layout algorithm (wiringLayout.js). Kept in its own
// import-free module so wiringLayout.js - and its node:test unit tests -
// never have to pull in the Vue components.

export const NODE_BOX_WIDTH = 240
export const NODE_BOX_HEIGHT = 76

// vim: set noet ts=4 sw=4:
